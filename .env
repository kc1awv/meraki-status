# Tune monitor parallelism (total in-flight pings)
PING_CONCURRENCY=30

# SQLite file in /data volume inside api container
SLA_DB=/data/sla.sqlite

# Expose API on host (override in compose via ports)
API_PORT=8080
